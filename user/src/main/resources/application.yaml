server:
  port: 7701
  servlet:
    context-path: /user
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
logging:
  level:
    com:
      tuberose:
        clock:
          user:
            mapper: trace